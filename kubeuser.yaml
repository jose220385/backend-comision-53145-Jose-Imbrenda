apiVersion: v1
kind: Service
metadata:
  name: kubeservice
spec:
  selector:
    app: kubeuser
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubeuser
spec:
  replicas: 5
  selector:
    matchLabels:
      app: kubeuser
  template:
    metadata:
      labels: 
        app: kubeuser
    spec:
      containers:
        - name: kubecontainer
          image: jose2203/operation:1.0.0
          ports:
            - containerPort: 8080
          env:
            - name: MONGO_URL
              value: mongodb+srv://jsimbrenda:4F4ZJdNwWpQy9kl1@papelerasangerardo.wphphau.mongodb.net/ecommerce?retryWrites=true&w=majority&appName=papeleraSanGerardo
            - name: ADMIN_MAIL
              value: adminCoder@coder.com
            - name: PORT
              value: "8080"
            - name: PERSISTENCE
              value: mongo
          imagePullPolicy: Always